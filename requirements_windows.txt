# QRFS toolchain requirements (Windows)
# Covers: qrfs_cli_ecc_lmh.py, create_slide_qrfs_v3.py, slice_slide_2x2_v2.py, rebuild_from_slide_v3.py
#
# Notes:
# - pyzbar needs ZBar runtime on Windows. If decode fails, install a ZBar build and ensure zbar DLL is on PATH.
# - cairosvg may require a Cairo runtime; if it gives you grief, you can still generate PDFs via svglib+reportlab.

Pillow>=10.0
numpy>=1.24
tqdm>=4.66

# QR generation
qrcode>=7.4
qrcodegen>=1.8
segno>=1.6

# Erasure coding
reedsolo>=1.7

# Decoders (optional but recommended)
opencv-python>=4.8
pyzbar>=0.1.9

# Vector/PDF exporters (optional but recommended)
cairosvg>=2.7
svglib>=1.5
reportlab>=4.0

# Lithography formats
gdstk>=0.9
gdspy>=1.6

# PDF input support (rebuild_from_slide_v3 --format pdf)
pymupdf>=1.23
